"""Personal job-application tracker.

Two entry points over one service layer (``tracker.services``):
  - FastAPI REST API (``tracker.api``) for the React web UI
  - ``tracker`` CLI (``tracker.cli``) for updating the tracker from the terminal
"""

__version__ = "0.1.0"
